{
  "name": "robotaspi",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "pins": "node scripts/gen-pins.mjs",
    "build": "node scripts/gen-pins.mjs && astro build",
    "preview": "astro preview",
    "generate": "node scripts/generate-article.mjs",
    "generate:batch": "node scripts/generate-article.mjs --batch"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.2.1",
    "@resvg/resvg-js": "^2.6.2",
    "astro": "^5.1.1",
    "gray-matter": "^4.0.3",
    "satori": "^0.29.0"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.68.0"
  }
}
